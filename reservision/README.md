# Reservision Frontend (Vue 3 + Vite)

## ✅ Prerequisites (install once)
1. **Node.js LTS** (includes npm)
2. **Git** (optional, only if you pull updates)

## ▶️ Run the Frontend (Windows)
1. Open PowerShell.
2. Go to the frontend folder:
   - `cd "C:\Users\John Rhey Tamares\CAPSTONE_FRONTEND\reservision"`
3. Install dependencies (first time or after pulling updates):
   - `npm install`
4. Start the dev server:
   - `npm run dev`
5. Open the app in your browser:
   - `http://localhost:5173`

## 🔗 Backend Connection
Make sure the backend API is running at `http://localhost:8000` (see backend README for steps).

---

## Vite Notes
This template uses Vue 3 `<script setup>` SFCs. For details, see the [script setup docs](https://v3.vuejs.org/api/sfc-script-setup.html#sfc-script-setup).

Learn more about IDE Support for Vue in the [Vue Docs Scaling up Guide](https://vuejs.org/guide/scaling-up/tooling.html#ide-support).# Vue 3 + Vite

To run:
Right click the "reservision" folder

type this command to install 

This template should help get you started developing with Vue 3 in Vite. The template uses Vue 3 `<script setup>` SFCs, check out the [script setup docs](https://v3.vuejs.org/api/sfc-script-setup.html#sfc-script-setup) to learn more.

Learn more about IDE Support for Vue in the [Vue Docs Scaling up Guide](https://vuejs.org/guide/scaling-up/tooling.html#ide-support).
